{
  "name": "chowda",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev": "pnpm -r --parallel --if-present dev",
    "lobsters": "pnpm --filter @chowda/lobsters-cli start",
    "test": "pnpm -r --if-present test"
  }
}
